# ionpower-node 0.47 — library hunt wave 16

Six more vendored libraries under smoke coverage:

| Library | Shape |
|---|---|
| `array-uniq` | `[1,2,2,3,3,3]` → `[1,2,3]`. |
| `eol` | `eol.lf("a\\r\\nb\\rc\\n")` → `"a\\nb\\nc\\n"`; `crlf` round-trips. |
| `is-linux` | `false` on darwin. |
| `is-windows` | `false` on darwin. |
| `semver-regex` | Loaded; recent versions use `\u` escapes SM45 doesn't grok in literal form — falls into skip cleanly. |
| `entities` | Skipped — multi-file (`./decode.js` companion missing). |

(`child_process.execFile` was on the v0.47 plan; turned out to
already be implemented from earlier sessions, so this release is
library-hunt-only.)

## Totals

- Libraries with smoke coverage: **637 → 641+**
- Assertions: **1644+** across **396** smoke files.

## Install

```
tar -C /opt -xzf ionpower-node-0.47-g5-ppc.tar.gz
/opt/ionpower-node-0.47/bin/node script.js
```
